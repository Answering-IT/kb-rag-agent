# Deprecated Scripts

Old scripts kept for reference. May not work with current infrastructure.

---

## ⚠️ Warning

These scripts are **not maintained** and may fail with current setup.

---

## Contents

- **test-agent.py** - Agent V1 tests (Agent V1 deprecated, use Agent V2)
- **test-ocr-agent.py** - OCR Lambda tests (archived)
- **test-api.py** - Old REST API tests
- **test-tools.py** - Old tool tests
- **test-agent-tools.sh** - Old shell tests
- **run-e2e-tests.sh** - Old E2E test runner

---

## Why Deprecated?

- Agent V1 replaced by Agent V2 (Agent Core Runtime)
- Old testing approaches replaced by `active/test-hierarchical-fallback.py`
- Infrastructure evolved (WebSocket API, new endpoints)

---

## If You Need These

Consider updating them or using equivalent from `active/` directory.

---

**For active scripts, see:** `../active/`
